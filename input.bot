token('xxxxxxxxxxxx')

counter = 1

.len()

command inc() {
    .len()
    global counter
    counter = counter + 1
}

command dec() {
    global counter
    counter = counter - 1
}

command show() {
    global counter
    send(counter)
}
